import { UpdateQuantityRequest } from './update-quantity-request';

describe('UpdateQuantityRequest', () => {
  it('should create an instance', () => {
    expect(new UpdateQuantityRequest()).toBeTruthy();
  });
});
